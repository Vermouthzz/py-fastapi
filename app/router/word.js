const { app } = require("egg-mock/bootstrap");

module.exports = (app) => {
  const { router, controller } = app;
  // router.get("/word", controller.word.index);
  // router.get("/word/:id", controller.word.show);
  // router.post("/word", controller.word.create);
  // router.put("/word/:id", controller.word.update);
  // router.get("/word/:id/like", controller.word.like);
};
